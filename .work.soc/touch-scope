# SOC-013 — declared touch scope
# One allowed path prefix per line. Trailing / allows everything under it.
# Operator-authorized iteration (same-message request 2026-08-10): verify/audit
# the deploy skills (soc-deploy-basic/-files/-repo), normalize argument forms
# (verb with/without `--`, path in any position), add `.cursorrules` deployment
# verification (SOC_SOURCE, skill handles, skeleton, sister frameworks), and
# resolve gaps found. Spans scripts, skills, templates, tests, and SOC docs by
# nature of the task.
#
# Protected files remain OUT of scope (pyproject.toml, Makefile,
# .pre-commit-config.yaml, uv.lock, .github/, Containers/Dockerfile, .env*).
#
# Blast-radius authorization for THIS iteration (operator, 2026-08-10,
# same-message "verify all deploy skills ... resolve any gaps or issues"):
# the task touches 8 top-level areas (scripts/, skills/, templates/, tests/,
# .quick/, .cursorrules, START_HERE.md, .work.soc/). Remove this marker line to
# restore the default-2 limit immediately.
# BLAST_RADIUS_MAX_AREAS: 8
scripts/
skills/
templates/
tests/
.quick/
.cursorrules
START_HERE.md
.work.soc/plans/
.work.soc/context/
.work.soc/touch-scope
