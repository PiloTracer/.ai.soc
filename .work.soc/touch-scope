# Declared touch scope — adopt-framework-improvements (2026-08-14)
# One allowed path prefix per line. Trailing / allows everything under it.
# Operator-authorized iteration (same-message request 2026-08-14): implement
# .work.soc/prompts/adopt-framework-improvements.md — Phase 1 operator handoff
# contract, Phase 2 document clarity contract, Phase 3 structural audit.
# Spans skills/ (contract + references), templates/ (doc headers),
# scripts/framework-verify.sh (enforcement), .cursorrules (communication
# section + SOC block sync), and .work.soc/ (scope + audit report).
#
# Also in the dirty tree (carried over, still uncommitted): the post-SOC-013
# stale-handle scoping fix in scripts/soc-deploy-basic.sh +
# tests/test_deploy_scripts.py, and the SOC-014 soc-session parity work.
# In scope so the gates see the full diff.
#
# Task inputs (operator-provided, not edited by this iteration):
# .work.soc/prompts/*.md (the four adoption/audit prompts + README).
# agent.os.framework.md (root; carried-over dirty-tree file, origin unknown
# to this iteration — included so the gate reflects the real diff; do not
# treat inclusion as authorization to modify it).
#
# Protected files remain OUT of scope (pyproject.toml, Makefile,
# .pre-commit-config.yaml, uv.lock, .github/, Containers/Dockerfile, .env*).
#
# Blast-radius authorization for THIS iteration (operator, 2026-08-14,
# same-message "make the soc-session works similarly to
# .ai/skills/session-control ... proceed unattended and autonomously"):
# the diff touches 8 top-level areas (scripts/, tests/, skills/, templates/,
# .quick/, .cursorrules, START_HERE.md, .work.soc/). Remove this marker line
# to restore the default-2 limit immediately.
# BLAST_RADIUS_MAX_AREAS: 8
scripts/
tests/
skills/
templates/
.quick/
.cursorrules
START_HERE.md
.work.soc/plans/
.work.soc/context/
.work.soc/reports/
.work.soc/prompts/
.work.soc/touch-scope
agent.os.framework.md
