# SOC-016 touch scope — remove soc-deploy-repo (skill + script) and refresh the
# framework docs/registries/tests. Operator-authorized 2026-08-19 in-message
# ("PROCEED WITH ALL NECESSARY CHANGES, GET RID OF deploy-repo!").
# Per-message only — NOT standing. No git commit/push requested.
#
# No protected files (pyproject.toml, Makefile, containers/, .github/,
# .pre-commit-config.yaml, .env*) are touched by this iteration.

# Deletions (explicitly requested in-message)
skills/soc-deploy-repo/
scripts/soc-deploy-repo.sh

# Registries + docs
.cursorrules
START_HERE.md
skills/README.md
skills/SKILL_DEPENDENCIES.md
skills/soc-deploy-files/
templates/cursorrules.soc.snippet.template
docs/homogenization/soc.md

# Scripts + tests
scripts/framework-verify.sh
scripts/soc-deploy-basic.sh
tests/test_deploy_scripts.py

# Scope declaration + session work tree
.work.soc/

# Reliability fix: commit-ref-pending bookkeeping must stay untracked
.gitignore

# Operator-authorized for SOC-016 removal iteration (2026-08-19, this message
# only): 9 top-level areas (.cursorrules, START_HERE.md, skills/, scripts/,
# templates/, docs/, tests/, .work.soc/, .gitignore — the last for the
# commit-ref-pending reliability fix). Remove the marker to restore the
# standing max-4 (SOC-011-gate).
# BLAST_RADIUS_MAX_AREAS: 9
