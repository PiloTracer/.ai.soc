# Security OS framework audit — clarity-contract adoption + structure

**Status:** Final · 2026-08-14 · **Needs:** nothing — all proposed fixes approved and applied (2026-08-14)

Audit per `.work.soc/prompts/framework-audit.md`, run after Phases 1–2 of
`.work.soc/prompts/adopt-framework-improvements.md`. Read-only; no fixes in
this report are applied.

## Scope

- Repo: `.ai.soc` (Security OS), framework source repo.
- Checks 1–8 from `framework-audit.md`: registry congruence, operator
  handoff adoption, document clarity adoption, routing docs,
  templates ↔ .cursorrules sync, hooks, protected surfaces, verifiers.

## Check 1 — Registry congruence — PASS

Six skills registered identically in all four places:
`.cursorrules` SOC skills table (`.cursorrules:251-257`), `skills/README.md:23-30`,
`skills/SKILL_DEPENDENCIES.md` dependency matrix, and `skills/<id>/`
directories (`soc-deploy-basic`, `soc-deploy-files`, `soc-deploy-repo`,
`soc-director`, `soc-gateway`, `soc-session`). Verified by
`scripts/framework-verify.sh` "Agent skills" section: `6 skills present`, exit 0.

## Check 2 — Operator handoff adoption — PASS

- Contract section present: `skills/SKILL_DEPENDENCIES.md:5` (`## Operator handoff contract`), canonically worded per the source `.ai` framework.
- Every `skills/<id>/skill.md` references it (grep `Operator handoff`: all 6 skills, ≥1 hit each; verified by `framework-verify.sh` "Clarity contracts (operator handoff)" — 7/7 OK).
- Operator-facing report templates close with Form A / Form B:
  - status template `skills/soc-session/skill.md:167` (Form A);
  - start report `skills/soc-session/reference.md:284`, context report `reference.md:345`, commit report `reference.md:439`, close report `reference.md:634` (Form A + Form-B fallback instructions);
  - deploy skills carry close instructions: `skills/soc-deploy-basic/skill.md:194`, `skills/soc-deploy-files/skill.md:147`, `skills/soc-deploy-repo/skill.md` (Next commands section), findings summary `skills/soc-director/skill.md` (Output section).
- `.cursorrules` synced: `.cursorrules:420` (Communication) and the SOC block `.cursorrules:261`.

## Check 3 — Document clarity adoption — PASS

- Contract section present: `skills/SKILL_DEPENDENCIES.md:39` (`## Document clarity contract`).
- Doc-generating skill references it: `skills/soc-session/skill.md` (`**Document clarity:**` bullet). soc-session is the only skill that authors/maintains documents (HANDOFF_SOC, NEXT_SOC, UNKNOWNS_SOC); the deploy/gateway/director skills emit chat reports only, and scan reports under `strix_runs/` are authored by the strix runtime, not by a skill. `DOC_GENERATING="soc-session"` at `scripts/framework-verify.sh:61`.
- Templates carry Status/Needs header + exactly one `## Next action`:
  `templates/work/context/HANDOFF_SOC.md.template:3,66`,
  `templates/work/plans/NEXT_SOC.md.template:3,63`,
  `templates/work/plans/UNKNOWNS_SOC.md.template:3,17`,
  `templates/work/analysis/README.md.template:3,49`,
  `templates/work/README.md.template:3,49`.
  Verified by `framework-verify.sh` "Doc template headers" — 10/10 OK.

## Check 4 — Routing docs — FAIL (4 stale references)

- F1 (confirmed): `templates/work/plans/NEXT_SOC.md.template:29` recommends `@soc-assessment run --target <path>` — no `soc-assessment` skill exists (registered set is the six in Check 1).
- F2 (confirmed): `templates/work/README.md.template:47` says "invoke `@soc-bootstrap init`" — no `soc-bootstrap` skill exists.
- F3 (confirmed): `templates/work/context/HANDOFF_SOC.md.template:35-37` points conditional reads at `.ai.soc/standards/*testing*`, `*compliance*`, `*threat-model*` — no `standards/` directory exists in this repo (`ls standards` → No such file or directory).
- F4 (confirmed): `skills/README.md:25` and `skills/soc-deploy-basic/skill.md` ("Explicitly NOT copied") describe `standards/**` / `concepts/**` as framework assets that stay in the source — neither directory exists here. Either the docs over-promise, or the asset trees were never ported from Agent OS.
- `START_HERE.md` itself is clean: every referenced file (`.quick/session-lifecycle.md`, `.quick/directors.md`, `.quick/deploy-to-project.md`) and skill exists.

## Check 5 — Templates ↔ .cursorrules sync — PASS

- Snippet template `templates/cursorrules.soc.snippet.template` matches the SOC block structure of the root `.cursorrules`; the new `**Reports:**` line was added to both (`.cursorrules:261`, snippet `:47`), so thin-client deploys carry the same handoff pointer.
- Placeholder maps agree (`{WORK_SOC_ROOT}` = `.work.soc/` etc. in both `.cursorrules` and `skills/SKILL_DEPENDENCIES.md`).
- The fat-client `.cursorrules` SOC block intentionally adds a `### SOC runtime` table absent from the thin snippet — pre-existing, by design (inference: thin targets get runtime info from the source at runtime).

## Check 6 — Hooks — PASS

`hooks/commit-msg.soc`, `post-commit.soc`, `pre-commit.soc`, `prepare-commit-msg.soc` all present and executable (`-rwxrwxr-x`). `scripts/install-git-hooks.sh` chains `.git/hooks/<hook>.*` dispatchers matching the `.soc` suffix; no references to removed hook scripts found.

## Check 7 — Protected surfaces — PASS with one observation

- `.cursorrules:290-295` protected list covers this framework's high-blast paths (`pyproject.toml`, `Makefile`, `containers/Dockerfile`, `.github/workflows/*`, `.pre-commit-config.yaml`, `.env*`).
- No separate protected-files JSON exists in this repo (unlike Agent OS) — single source of truth, so no drift between lists. Not a gap per the audit rule, but recorded: if a JSON list is introduced later, it must be kept congruent.
- F5 (confirmed, minor): the list spells `Containers/Dockerfile` (`.cursorrules:292`) but the real path is lowercase `containers/Dockerfile`. On case-sensitive filesystems a case-sensitive matcher would miss it.

## Check 8 — Verifiers — PASS

| Verifier | Result |
|----------|--------|
| `bash scripts/framework-verify.sh` | exit 0 — all checks passed, including the new contract/template checks added in Phases 1–2 (`scripts/framework-verify.sh:40,55,69`) |
| `bash scripts/touch-scope-verify.sh` | FAIL → PASS after scope update (see below) |
| `bash scripts/blast-radius-check.sh` | PASS — `files=24 areas=6 risk=med (max=8 via touch-scope)` |
| `bash scripts/gate-verify.sh` | PASS |

Initial `touch-scope-verify` failure was not an adoption defect: the dirty
tree contained operator-provided task inputs (`.work.soc/prompts/*.md`) and a
carried-over root file (`agent.os.framework.md`) not yet declared in
`.work.soc/touch-scope`. Scope declaration updated to cover them; re-run
passes.

## Findings (fixes approved by operator 2026-08-14 — APPLIED)

| # | Class | Finding | Applied fix |
|---|-------|---------|-------------|
| F1 | confirmed | `@soc-assessment run` did not exist | `templates/work/plans/NEXT_SOC.md.template` — row now points at `@soc-director - scan <target>` |
| F2 | confirmed | `@soc-bootstrap init` did not exist | `templates/work/README.md.template` — now `@soc-deploy-basic update` |
| F3 | confirmed | `.ai.soc/standards/*` referenced, directory absent | `templates/work/context/HANDOFF_SOC.md.template` — conditional reads repointed at `skills/soc-director/skill.md` and the `.work.soc/analysis/` template |
| F4 | confirmed | `standards/**` / `concepts/**` described as source assets, absent on disk | Trimmed to reality (skills/scripts/templates) in `skills/README.md`, `skills/soc-deploy-basic/skill.md`, `skills/soc-deploy-files/skill.md`, `templates/work/README.md.template`, `templates/cursorrules.soc.snippet.template` — decided: trim docs, not port trees |
| F5 | confirmed | `Containers/Dockerfile` vs actual `containers/Dockerfile` | Case fixed in `.cursorrules` protected-files list |

Verified after fixes: `grep -rn "standards\|soc-assessment\|soc-bootstrap" skills/ templates/ START_HERE.md .cursorrules .quick/` — no stale hits (the one remaining `concepts` hit is `.cursorrules:157` `{UI_CONCEPTS_ROOT}`, a legitimate pointer into the sibling `.ai.ui` framework).

No `inference`-class adoption gaps remain. **Unknown (unverified):** whether
deployed thin/fat targets generated before this change carry stale
`.work.soc/` skeletons — they would pick up the new templates on the next
`@soc-deploy-basic update` / `@soc-deploy-files update`; not verified against
a live target in this run.

## Next action

Next action: none — findings applied; verification green (see § Check 8).
