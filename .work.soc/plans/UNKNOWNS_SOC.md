# UNKNOWNS_SOC — SOC unknowns registry

**Updated:** 2026-07-27 · **Maintained by:** soc-bootstrap / soc-assessment

| ID | Question / blocker | Blocks | Owner | Status |
|----|-------------------|--------|-------|--------|
| U-SOC-1 | | | | Open |
| U-SOC-04 | Which keyring dependency (`keyring`, `keyrings.cryptfile`, or platform-specific) should back SOC-008-J's encrypted `~/.strix/cli-config.json` storage? Adding any of them changes `pyproject.toml` (protected) and isn't a unilateral decision. | SOC-008-J | Operator | Open |
| U-SOC-05 | Exact CPU/memory limits (if any) applied to sandbox containers by the pinned `openai-agents==0.14.6` SDK's `DockerSandboxClient` base implementation — this repo's own code (`strix/runtime/docker_client.py`) never sets `security_opt`/resource limits explicitly, so the effective default comes from a dependency not fully read line-by-line. | SOC-008-I | — | Open |
| U-SOC-06 | Should `--fail-on`/SOC-008-F's severity ranking, SOC-008-E's SARIF severity mapping, or SOC-009's per-run `strix.log` output be exercised against a real, live scan (not just unit tests) before being relied on for operator workflows or CI? No end-to-end scan was run in SOC-008 or SOC-009. | Confidence in SOC-008-E/F and SOC-009 logging for real use | — | Open |
| U-SOC-07 | Did any session before 2026-07-18 rely on `uv run mypy`/`uv run bandit`/`uv run pytest` output while the `.venv` script shebangs were broken (see HANDOFF_SOC "Key findings," SOC-008)? If so, that session's "checks passed" claims may have been silently wrong (bandit/mypy failed outright; pytest ran the wrong interpreter). Not something this session can retroactively verify. | Trust in any pre-2026-07-18 `uv run`-based verification claims | — | Open — informational, not blocking new work |
| U-SOC-08 | Does auto-defaulting `--output-dir` to `<local-target>/.work.soc` cover every operator workflow (URL-only targets, multi-target scans, resume without local path)? URL scans still require explicit `--output-dir`; not live-tested in SOC-009. | Confidence SOC-009 output routing matches operator expectations | — | Open |
| U-SOC-09 | Should SOC-011 #3 have ALSO shipped automatic sandboxed PoC re-execution (the original proposal mentioned a verification subagent)? Current implementation makes verification FIRST-CLASS but ASSERTION-BASED only (agent sets `verified`/`verification_evidence` at file time; operator opts into filtering via `--exclude-unverified`). Re-execution would add LLM cost per finding, risk of mutating the live target app, and a brittle assumption that `poc_script_code` is self-contained. Whether this trade-off is the right default is an operator decision, not a unilateral one. | Confidence SOC-011 #3 is the optimal verification model vs. automatic re-execution | Operator | Open — narrowed by SOC-012. The weaker half of this concern is closed: `verified` no longer defaults to `true`, so SARIF can no longer carry an unauditable reproduction claim, and any assertion (true or false) now requires `verification_evidence`. What remains genuinely open is whether assertion-with-evidence is sufficient or whether sandboxed PoC re-execution is still wanted. |
| U-SOC-10 | With `verified` now defaulting to "not asserted", `--exclude-unverified` will filter out every finding until agents actually populate the field — meaning a CI job using the flag can exit 0 on a run with real findings. SOC-012 mitigates this with a printed count of dropped findings, but should the flag instead exit non-zero (or warn harder) when it drops everything? | Confidence `--exclude-unverified` is safe to recommend for CI | Operator | Open |

## Review log

| Date | Reviewer | Action |
|------|---------|--------|
| 2026-06-30 | bootstrap | Initial template |
| 2026-07-18 | SOC-008 session | Added U-SOC-04 (keyring choice, blocks SOC-008-J), U-SOC-05 (sandbox resource limits, blocks SOC-008-I), U-SOC-06 (no live-scan verification of E/F), U-SOC-07 (retroactive trust question re: the broken `uv run` toolchain found and fixed this session) |
| 2026-07-20 | SOC-009 session | Extended U-SOC-06 to include SOC-009 `strix.log`; added U-SOC-08 (URL/multi-target output-dir auto-default not live-tested) |
| 2026-07-27 | SOC-010 session | U-SOC-08 narrowed: SOC-010 closed the URL-default gap (`<cwd>/.work.soc` now used when no `--output-dir` and no local target inferred). What remains unverified is the *live Docker-bootstrapped* path for URL targets — static + unit verification covers the resolution ladder and run-name format, but no real scan wrote `strix.log` under `<cwd>/.work.soc/strix_runs/<run-name>/` this session. U-SOC-04 (keyring choice) and U-SOC-05 (sandbox resource limits) unchanged — both still block SOC-008-J / SOC-008-I. |
| 2026-07-27 | SOC-012 session | Independent review of the uncommitted SOC-011 diff found 7 issues; 6 fixed (see NEXT_SOC SOC-012 row). U-SOC-09 narrowed — the "unauditable `verified: true`" half is closed by defaulting the field to not-asserted and requiring evidence for either assertion. Added U-SOC-10 (the `--exclude-unverified` silent-pass question that the new default creates). U-SOC-06 partially narrowed: the runner→synthesis call-site wiring is now covered by two tests (positive + negative), so what remains unverified is genuinely the live Docker/LLM path, not the branch logic. |
| 2026-07-27 | SOC-011 session | Added U-SOC-09 — should SOC-011 #3 also have shipped automatic sandboxed PoC re-execution as a subagent (the original proposal)? Current implementation is assertion-only + opt-in filter; re-execution was deferred as adding LLM cost, target-mutation risk, and a brittle self-containedness assumption. Operator decision. Live E2E (U-SOC-06/08) unchanged — SOC-011 is unit/static-verified only, no real agent cycle exercised. |
| 2026-07-27 | SOC-011-gate session | Operator granted standing blast-radius approval (max-4) — operationalized via `# BLAST_RADIUS_MAX_AREAS: N` marker in `.work.soc/touch-scope`, consumed by `scripts/blast-radius-check.sh`. Marker is visible/traceable in git history (not a shell env var) and removable in one line to restore default-2. Default-2 unchanged when marker absent; protected-file rule and DLP per-message rule remain in force. |
| 2026-08-19 | SOC-015 session | Homogenization applied + verified (see NEXT_SOC SOC-015 row). No new unknowns. U-SOC-04/05/06/08/09/10 unchanged. Orchestrator anchor confirmed: `../pilo.ai.logicbison` present, `../.ai` absent — no operator action needed. |
