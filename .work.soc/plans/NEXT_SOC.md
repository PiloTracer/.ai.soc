# NEXT_SOC — Security OS tactical next action

## Recommended next

```
1. [NEXT] Run a live end-to-end scan (real repo or URL target) to confirm strix.log lands under target .work.soc and observe SOC-008-A/D/E behavior for real
2. [NEXT] SOC-008-I — sandbox hardening (Dockerfile NOPASSWD:ALL, always-on NET_ADMIN/NET_RAW, no resource limits) — needs a dedicated session with Docker build+smoke-test time budgeted (U-SOC-05 open). Deferred again in SOC-010 — changes to the attacker-facing sandbox image without a Docker build+smoke budget could silently break the running agent, which conflicts with the operator's "keep this framework easy to use" criterion.
3. [NEXT] SOC-008-J — encrypt-at-rest for ~/.strix/cli-config.json — needs operator's keyring-library choice before any code is written (U-SOC-04 open). Declined in SOC-010 — adds a dependency pin to protected `pyproject.toml` AND new operator-facing init/set-up UX, both of which add difficulty contrary to the SOC-010 task brief.
4. [DONE in SOC-010] Address path rebasing for `.cursorrules` (replace `/mnt/work/Projects/.ai*` with `{WORK_ROOT}/.ai*` + `WORK_ROOT:` resolution rules). Note: `.ai/opencode.json` already uses relative `../.ai.soc` paths and is portable as-is; consumer-project `opencode.json` (e.g. system-erp/opencode.json) is the consumer's responsibility, not `.ai.soc`'s.
5. [PENDING] Optionally rename Python module `strix/` → `soc/` — operator declined in SOC-010 ("may break the code, leave untouched").
6. [DONE in SOC-011-gate] Operator granted standing blast-radius approval (max-4) — operationalized as a `# BLAST_RADIUS_MAX_AREAS: N` marker in `.work.soc/touch-scope` consumed by `scripts/blast-radius-check.sh`; removable in one line. Default-2 unchanged when marker absent; protected-file rule still in force; DLP per-message rule still in force.
```

## Current SOC iteration

| ID | Description | Status |
|----|-------------|--------|
| SOC-001 | License audit — Apache 2.0 → `.ai.soc` adoption | DONE |
| SOC-002 | Branding & identity migration (Strix → .ai.soc) | DONE |
| SOC-003 | NOTICE file + modification notices on all changed files | DONE |
| SOC-004 | Bootstrap templates for `.work.soc/` structure | DONE |
| SOC-005 | Path templating for portable `.ai.soc` framework | DONE in SOC-010 (`.cursorrules` portion) |
| SOC-006 | Register `.ai.soc` skills with `opencode.json` | DONE |
| SOC-007 | Remove tools-project integration (moved to parent `.ai`) | DONE |
| SOC-008 | Tool improvement plan (auth gate, honest scope language, secret scrubbing, SSRF guard, SARIF export, `--fail-on`, `make test`) — sub-items A–H | A,B,C,D,E,F,H DONE (uncommitted) · G declined by operator · I,J deferred |
| SOC-009 | Scan logging lifecycle + target `.work.soc` default output dir + `strix.log` in run dir | DONE (uncommitted, verified 2026-07-20) |
| SOC-010 | URL/IP/repo default output dir to `<cwd>/.work.soc` + run-name format regression tests + `.cursorrules` path templating (SOC-005) — verified 2026-07-27 | DONE (uncommitted) |
| SOC-011 | Strix scanning-level improvements: finish-scan reliability synthesis + per-scan-mode vulnerability-class checklist + verification first-class (`verified`/`verification_evidence` fields + `--exclude-unverified` CLI flag) — verified 2026-07-27 | DONE (committed `9f53514`) |
| SOC-011-gate | Operator standing blast-radius approval made visible+removable: `scripts/blast-radius-check.sh` reads `# BLAST_RADIUS_MAX_AREAS: N` from `.work.soc/touch-scope` (default 2 unchanged); `--self-test` short-circuits before reading scope | DONE (uncommitted) |
| SOC-012 | Review-and-fix pass over the uncommitted SOC-011 diff — 7 issues found, 6 fixed, 1 (live E2E) still needs a real scan — verified 2026-07-27 | DONE (uncommitted) |
| SOC-013 | Deploy-skill audit: argument normalization (verb ±`--`, path any position) across all three deploy scripts; new `verify` mode auditing target `.cursorrules` (SOC_SOURCE, stale skill handles, skeleton, sister frameworks); fat-client SOC_SOURCE now points at the local copy; deploy scripts included in fat copies — verified 2026-08-10 | DONE (committed `fefbb61`) |
| SOC-013-fix | Post-commit review fix: stale-handle check in `soc-deploy-basic.sh verify` now scoped to the SOC block only (sister frameworks legitimately use bare `deploy-basic` outside it); regression test added | DONE (committed in SOC-014 close) |
| SOC-014 | soc-session parity with `.ai/skills/session-control`: added standalone `commit`/`commit push` (no close), `close commit scoped`, `close push` → commit push normalization, task-ref extraction + `.work.soc/active-ref`, secrets-scan halt on close, per-mode completion checklists, start marks HANDOFF_SOC Open; new `reference.md` with detailed protocols; registry/docs/templates aligned; **commit scope made mode-dependent per operator (2026-08-14): full repo in the framework source repo, `.work.soc/` only in deployed targets** | DONE (committed in SOC-014 close) |
| SOC-015 | Homogenization per `docs/homogenization/soc.md`: shared `scripts/sister-discovery.sh` (six `.ai.<fw>` slots, legacy + `pilo.ai.<fw>.logicbison` family naming with slot-replace); `soc-deploy-basic.sh verify` six-slot sister loop + Agent OS anchor check (`.ai` / `pilo.ai.logicbison`); `.cursorrules` + snippet template carry the 7-row framework-paths table (targets can route cross-framework); install-git-hooks prose refreshed; orchestrator anchor resolved (`../pilo.ai.logicbison` present, `../.ai` absent); release **0.6.1** (pyproject + uv.lock + README version line) — verified 2026-08-19 | DONE (committed in SOC-015 close) |

## Completed work

| Item | Detail |
|------|--------|
| NOTICE file | `NOTICE` at repo root with attribution + change log |
| Package rename | `pyproject.toml`: `strix-agent` → `ai-soc`, CLI `soc` added |
| .cursorrules identity | Updated from `strix-agent` to `.ai.soc` |
| README | Rebranded, removed Strix logos/badges, added attribution |
| Docs (25 .mdx files) | Product name + CLI command updated |
| Logo removal | `.github/logo.png`, `.github/screenshot.png`, `docs/images/*` removed |
| Gateway script | `gateway.sh` APP renamed, env vars preserved; `scripts/install.sh` removed (not needed — install via `uv sync`) |
| Templates | `templates/work/` + `templates/bootstrap.sh` created |
| Modification notices | Added to every changed file; `NOTICE` lists all modifications |
| soc-session skill | `skills/soc-session/skill.md` created; `.cursorrules` SOC section updated |
| SOC-010 URL default | URL/IP/repo targets without a local inference + without `--output-dir` now default to `<cwd>/.work.soc/strix_runs/<run-name>` (previously fell through to bare `<cwd>/strix_runs/<run-name>`). Operator-supplied `--output-dir` remains authoritative. Resume adds `<cwd>/.work.soc` first then legacy `<cwd>` as candidate bases so pre-SOC-010 URL runs stay resumable. — `strix/core/paths.py` (`configure_scan_output_dir` fallback, `output_dir_candidates` ordering) |
| SOC-010 run-name tests | `tests/test_run_name_format.py` pins operator-spec'd `YYYYMMDD-<slug>_<4hex>` format against the operator's exact example inputs (`/mnt/work/Projects/system-erp` → `-system-erp_<4hex>`; `http://localhost:13000` → `-localhost-13000_<4hex>`). `tests/test_scan_output_dir.py` extended with 4 new tests for the URL default + explicit-override + legacy resume discovery. |
| SOC-005 `.cursorrules` templating | Absolute `/mnt/work/Projects/.ai*` paths in `.cursorrules` replaced with `{WORK_ROOT}/.ai*` placeholders. A `WORK_ROOT:` resolution ladder (explicit `WORK_ROOT:` line → inferred parent of `.ai.soc` repo → `framework not installed here`) was added to the "Framework paths" section. `{UI_SKILLS_ROOT}`, `{UI_CONCEPTS_ROOT}`, `{SKILLS_ROOT}` (biz) placeholder resolutions updated to use `{WORK_ROOT}/...`. No user-facing complexity added: agents substitute `{WORK_ROOT}` at read time using the same convention `SOC_SOURCE` already established. |
| SOC-011 finish-scan synthesis | `strix/core/runner_completion.py` (new) + `strix/core/runner.py` — when a non-interactive scan ends with a text-only final turn (no `scan_completed=true`) AND ≥1 finding was filed via `create_vulnerability_report`, the runner now delegates to `synthesize_completion_from_findings()` which writes a minimal four-section executive report through the normal `ReportState.update_scan_final_fields` path. Executive report, SARIF, `vulnerabilities.<md\|json\|csv>`, and `run.json` all write the same way a real `finish_scan` call would. `run.json` carries a `synthesized_completion` marker `{source, filed_finding_count, final_output_preview, synth_id}` so a reader can tell a real finish from the fallback. No-op when no findings filed (refuses to fabricate an "all clear" from zero data) or no global `ReportState`. Tests: `tests/test_finish_scan_synthesis.py` (8). |
| SOC-011 per-mode checklist | `strix/core/inputs.py` — `get_scan_mode_checklist` + `_format_checklist_block` + `_SCAN_MODE_CHECKLISTS` table (quick=A01–A03, standard=A01–A07, deep=A01–A10 + `BIZ` for business-logic & race conditions; deep ⊃ standard ⊃ quick on a single coverage axis; unknown modes fall back to `standard` permissively). `build_root_task` now injects the checklist block (gated on having ≥1 real target — preserves the pre-SOC-011 contract that an empty scan_config returns `""`). Each checklist entry instructs the agent to populate `vulnerability_class` on filed findings so the coverage report downstream has a back-link. Tests: `tests/test_per_mode_checklist.py` (19). |
| SOC-011 verified first-class | `strix/report/state.py` — `add_vulnerability_report` accepts `vulnerability_class`, `verified`, `verification_evidence`. `verified` is persisted as `null` when not supplied (distinguishes "agent did not assert" from "agent asserted false"). `vulnerability_class` is uppercased (so `a03`/`A03` don't fragment into two coverage bins). `strix/tools/reporting/tool.py` — `create_vulnerability_report` exposes the three new params with `verified: bool = None` default per operator revision (no silent assertion); validation requires non-empty `verification_evidence` whenever `verified` is non-None in EITHER direction (True needs PoC-proof; False needs attempt-failure record); rejects `vulnerability_class` starting with a digit. `strix/report/writer.py` — SARIF `result.properties` now carries `verified`, `vulnerability_class`, `verification_evidence` when present, alongside the existing `security-severity`. `strix/interface/main.py` — new `--exclude-unverified` CLI flag (default OFF preserves today's count-everything behavior; opt-in filters unverified-or-absent `verified` findings from `should_fail_on_severity`); operator-revised length-mismatch policy fails CLOSED in EITHER direction; silent-pass guard prints dropped-finding count. Tests: `tests/test_verify_finding_fields.py` (15), `tests/test_sarif_export.py` (+6 new), `tests/test_fail_on_threshold.py` (+13 new). |
| SOC-011-gate override | Operator granted standing approval to operate across up to 4 top-level areas per iteration (rather than ad-hoc per-session authorization). Operationalized: `scripts/blast-radius-check.sh` reads `# BLAST_RADIUS_MAX_AREAS: N` from `.work.soc/touch-scope`; override marker logged in gate output whenever active; removing the marker restores default-2 immediately. `--self-test` short-circuits before reading scope (preserves unit-test contract). No behavioral change for legacy callers; default-2 unchanged when marker absent. |

| SOC-012 review fixes | Independent verification of the SOC-011 diff surfaced 7 issues; 6 were fixed in this pass. (1) `verified` no longer defaults to `True` — it defaults to "not asserted" (`None`) and an assertion in EITHER direction now requires `verification_evidence`, so SARIF can never carry a reproduction claim nobody can audit (`strix/tools/reporting/tool.py`). (2) `update_scan_final_fields` gained a `synthesized` flag written INSIDE `scan_results`, so a salvaged run no longer reports a bare `success: true` that a CI consumer would read as a clean finish; telemetry `exit_reason` is now `synthesized_from_findings` for that path (`strix/report/state.py`, `strix/core/runner_completion.py`). (3) `should_fail_on_severity`'s length guard changed from `<` to `!=` — a too-long `verified_flags` list previously escaped the guard and raised `ValueError` out of `zip(strict=True)` (`strix/interface/main.py`). (4) Checklist prompt text fixed: balanced parenthesis, RST double-backticks removed (the model saw them literally), and a proper blank-line separator matching every other root-task section (`strix/core/inputs.py`). (5) `touch-scope-verify.sh` and `blast-radius-check.sh` now include untracked files via `git ls-files --others --exclude-standard` — both previously ignored brand-new files, which is precisely the out-of-scope addition they exist to catch. (6) `.work.soc/touch-scope` no longer declares `.cursorrules`-protected files (`pyproject.toml`, `Makefile`, `.pre-commit-config.yaml`, `.github/`, `uv.lock`) as in-scope; declaring them would let the gate go green on the exact edit the protection forbids. Also added an operator-visible printed count when `--exclude-unverified` drops findings, so the flag can't cause a silent exit-0. Tests: 221 → 229. |
| SOC-013 deploy-skill audit + verify | All three deploy scripts now normalize arguments: verbs (`status`/`verify`/`update`/`force`/`clone`/`archive`) accept an optional `--` prefix and may appear before or after the target path, so `soc-deploy-basic.sh /path update` ≡ `/path --update` ≡ `--update /path`; verb-only forms operate in-place. New **`verify` mode** in `scripts/soc-deploy-basic.sh` (delegated to by `soc-deploy-repo.sh verify`) audits a deployed target read-only: `.cursorrules` + SOC block presence, stale pre-0.5.0 skill handles (`session-soc`, unprefixed `deploy-*`), `SOC_SOURCE` placeholder/unreachable/no-skills-registry (backtick-quoted pointers tolerated), thin/fat consistency (fat = local `.ai.soc/skills/` or repo-root framework → pointer unset or local), `.work.soc/` skeleton files, SOC block sections (warn), sister frameworks under resolved WORK_ROOT (info). Every basic/files deploy+update and every repo archive now ENDS with a verify pass and exits non-zero if it fails. **Fat-client pointer fix:** in-place `soc-deploy-files` previously wrote `SOC_SOURCE` at the ORIGINAL source (fat targets behaved thin); it now points at the local `<target>/.ai.soc`, and deploy scripts are included in fat copies so targets self-verify/self-update. Snippet template gained a "Verify deployment" line so reading `.cursorrules` in a target surfaces the audit command. Docs updated: 3 skill.md parse tables + completion checks, `skills/README.md`, `.quick/deploy-to-project.md`, `START_HERE.md`, `.cursorrules` SOC skills table. Live read-only audit of `/mnt/work/Projects/system-erp` (operator's example) correctly flagged its pre-0.5.0 stale block (`@session-soc`, `@deploy-basic`) while confirming its `SOC_SOURCE` and skeleton — target NOT modified (no operator request). Tests: `tests/test_deploy_scripts.py` (23) — normalization equivalence, verify pass/fail modes, fat self-containment, repo archive+verify, master self-verify. 229 → 252 total. |
| SOC-014 soc-session parity | `skills/soc-session/skill.md` rewritten as a behavioral mirror of `.ai/skills/session-control`, scoped to `.work.soc/`: same verb set (`start`/`close`/`status`/`context` + standalone `commit`/`commit push`), same modifiers (`close commit [scoped] [push]`, `close push` normalized to commit push), same report shapes with per-mode completion checklists. New `skills/soc-session/reference.md` carries the detailed protocols (S1–S6 start, X1–X3 context, M1–M6 commit, C1–C8 close), task-ref extraction priority (HANDOFF_SOC → `.work.soc/active-ref` → task-registry → branch → last commit → ask once → `type:` fallback per `.cursorrules` §Task Refs), secrets-scan halt, HANDOFF_SOC session-status templates (both the template shape and this repo's legacy header shape). SOC-specific guarantees kept: DLP per-invocation git permission, push-transports-branch caveat, mode-dependent commit scope (operator-specified 2026-08-14): framework-mode (repo root has `skills/soc-session/skill.md` + `scripts/soc-deploy-basic.sh`) commits the full repo — all modified/added/new files; target-mode (deployed thin/fat project) commits `.work.soc/` only. Secrets-scan halt applies in both modes; `git add -A` is sanctioned only in framework-mode. Aligned: `skills/README.md`, `skills/SKILL_DEPENDENCIES.md` (commit verb + gate row), `.quick/session-lifecycle.md`, `.cursorrules` + `templates/cursorrules.soc.snippet.template` skill tables, `START_HERE.md` quick reference, `templates/work/context/HANDOFF_SOC.md.template` (stale `@session-control` handles → `@soc-session`). |

## What was intentionally NOT changed

| Item | Reason |
|------|--------|
| `STRIX_LLM` env var | Python code reads it (`strix/config/settings.py`) — not trademark use |
| `strix/` module directory | Python import path — internal only, not brand presentation |
| `strix_runs/` output directory | Python code writes to it — internal only |
| `~/.strix/` config directory | Python code reads from it — internal only |

## Unknowns

| ID | Question | Owner |
|----|----------|-------|
| U-SOC-01 | Does `.ai.soc` name collide with any existing `.work.*` in sibling projects? | — |
| U-SOC-02 | Should `.ai.soc` be a framework directory (like `.ai/`) or just a skill set? | — |
| U-SOC-03 | Should env vars be renamed (STRIX_LLM → SOC_LLM) in Python code too? That's a separate code migration. | — |
